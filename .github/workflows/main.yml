name: CI

on:
  pull_request:
    types: opened
  push:
    paths:
      - .github/workflows/main.yml
      - .eslintrc
      - .eslintignore
      - tsconfig.json
      - package.json
      - bun.lockb
      - '*.ts'

jobs:
  test:
    uses: winstxnhdw/actions/.github/workflows/bun.yml@main
